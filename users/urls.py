from django.urls import path
from users import views

urlpatterns = [
    # path('users/', views.UserView.as_view()),
    # path('users/<int:user_id>/', views.UserDetailsView.as_view()),
    #
    # path('follows/', views.FollowView.as_view()),
    # path('follows/<int:follow_id>/', views.FollowDetailsView.as_view()),
    #
    # path('blocks/', views.BlockView.as_view()),
    # path('blocks/<int:block_id>/', views.BlockDetailsView.as_view()),
]
